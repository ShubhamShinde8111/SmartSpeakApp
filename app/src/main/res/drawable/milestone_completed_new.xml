<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer glow effect -->
    <item android:top="2dp" android:left="2dp" android:right="2dp" android:bottom="2dp">
        <shape android:shape="oval">
            <solid android:color="@color/milestone_glow" />
            <size android:width="44dp" android:height="44dp" />
        </shape>
    </item>
    
    <!-- Main completed circle -->
    <item android:top="4dp" android:left="4dp" android:right="4dp" android:bottom="4dp">
        <shape android:shape="oval">
            <solid android:color="@color/progress_completed" />
            <size android:width="40dp" android:height="40dp" />
            <stroke android:width="3dp" android:color="@android:color/white" />
        </shape>
    </item>
    
    <!-- Checkmark background -->
    <item android:top="12dp" android:left="12dp" android:right="12dp" android:bottom="12dp">
        <shape android:shape="oval">
            <solid android:color="@android:color/white" />
            <size android:width="16dp" android:height="16dp" />
        </shape>
    </item>
</layer-list>
